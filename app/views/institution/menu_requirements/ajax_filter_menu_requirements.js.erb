( function ajax( ) {
  const parentTable = document.querySelector( '#col_mr .parent_table' );

  <% if @menu_requirements.present? %>
    parentTable.innerHTML = '<%= j render( "table_menu_requirements" ) %>';
    MyLib.tableSetSession( parentTable );
  <% else %>
    parentTable.innerHTML = '';
  <% end %>
}( ) );
