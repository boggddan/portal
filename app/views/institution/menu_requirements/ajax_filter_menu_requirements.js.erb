( function( ) {
  const $blockTable = $( '#col_mr' ).children( '.parent_table' ).empty( );

<% if @menu_requirements.present? %>
  $blockTable.append( '<%= j render( "table_menu_requirements" ) %>' );
  MyLib.tableSetSession( $blockTable );
<% end %>
} ).call( this );
