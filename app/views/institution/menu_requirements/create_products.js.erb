( function ajax( ) {
  let elements = '';
  <% if @menu_products.present? %>
    elements = '<%= j render( 'table_menu_products' ) %>'
  <% end %>

  objMenuRequirementProducts.colPrInit( elements );
  objMenuRequirementProducts.buttonColPr.click( );
}( ) );
